def merge_dict(dict1, dict2):
    if dict1 is None:
        return dict2 or {}
    if dict2 is None:
        return dict1 or {}
    new_dict = dict1.copy()
    for key, value in dict2.items():
        if value is not None:
            new_dict[key] = value
    return new_dict
